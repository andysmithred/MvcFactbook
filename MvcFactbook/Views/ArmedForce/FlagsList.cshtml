@model MvcFactbook.ViewModels.Models.Main.ArmedForceView

@{
    ViewData["Title"] = "Details";
}

<br />
<partial name="DetailsMenu" />
<br />
<partial name="DetailsHeader" />

<div class="card border-0">
    <div class="card-header bg-secondary text-white">Places</div>
    <div class="card-body bg-dark p-0">
        <partial name="PartialListDetailsFlags" model="@Model.Flags.OrderBy(x => x.ListName)" />
    </div>
</div>