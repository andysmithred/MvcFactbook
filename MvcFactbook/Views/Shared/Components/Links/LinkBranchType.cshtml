@model MvcFactbook.ViewModels.Models.Main.BranchTypeView

@if (Model != null)
{
    <partial name="LinkItem" model='new {
             Controller = "BranchType",
             Action = "Details",
             RouteId = Model.Id,
             Icon = Model.IconLightPath,
             Label = Model.Type
            }' />
}
