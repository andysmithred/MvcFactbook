@model MvcFactbook.ViewModels.Models.Main.ShipSubTypeView

@if (Model != null)
{
    <partial name="LinkItem" model='new {
             Controller = "ShipSubType",
             Action = "Details",
             RouteId = Model.Id,
             Icon = "/images/icons/shipsubtype-light.png",
             Label = Model.Type
            }' />
}
