@model MvcFactbook.ViewModels.Models.Main.ShipbuilderView

@if (Model != null)
{
    <partial name="LinkItem" model='new {
             Controller = "Shipbuilder",
             Action = "Details",
             RouteId = Model.Id,
             Icon = "/images/icons/organization-light.png",
             Label = Model.ShortName
    }' />
}
