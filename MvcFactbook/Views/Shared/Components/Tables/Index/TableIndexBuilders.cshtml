@*@model IEnumerable<MvcFactbook.ViewModels.Models.Main.BuilderView>

<div>
    @(Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add().Encoded(false).RenderedAs(model => $"<img class='rounded' src='{model.ImageSource}' alt='Card image' style='height:20px;width:20px;object-fit:cover;overflow:hidden;opacity:0.4'>");
            columns.Add(model => model.ShortName).Titled("Short Name");
            columns.Add(model => model.Name).Titled("Name");
            columns.Add(model => model.Location).Titled("Location");
            columns.Add(model => model.FoundedLabel).Titled("Founded");
            columns.Add(model => model.DefunctLabel).Titled("Defunct");
            columns.Add(model => model.Ships.Count).Titled("Ships");
            columns.Add(model => model.Complete).Titled("Complete");
            columns.Add().Encoded(false).RenderedAs(model => $"<img src='/images/icons/{model.CompleteIcon}' style='height: 20px; width: 20px' />");
            columns.Add().Encoded(false).RenderedAs(model =>
                "<div class='btn-group float-right'>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/Builder/Edit/{model.Id}'>Edit</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/Builder/Details/{model.Id}'>Details</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/Builder/Delete/{model.Id}'>Delete</a>" +
                "</div>"
            );
        })
        .Empty("No data found")
        .Filterable()
        .Sortable()
        .Pageable(pager =>
        {
            pager.RowsPerPage = 14;
        })
        .AppendCss("table-hover")
    )
</div>*@
