@model IEnumerable<MvcFactbook.ViewModels.Models.Main.PoliticalEntityEraView>

<div>
    @(Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add().Encoded(false).RenderedAs(model => $"<img src='/images/icons/time-light.png' style='height: 20px; width: 20px' />");
            columns.Add(model => model.PoliticalEntity.Name).Titled("Political Entity");
            columns.Add(model => model.StartDate).Titled("Start date").Formatted("{0:d}");
            columns.Add(model => model.EndDate).Titled("End date").Formatted("{0:d}");
            columns.Add(model => model.Description).Titled("Description");
            columns.Add().Encoded(false).RenderedAs(model =>
                "<div class='btn-group float-right'>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntityEra/Edit/{model.Id}'>Edit</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntityEra/Details/{model.Id}'>Details</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntityEra/Delete/{model.Id}'>Delete</a>" +
                "</div>"
            );
        })
        .Empty("No data found")
        .Filterable()
        .Sortable()
        .Pageable(pager =>
        {
            pager.RowsPerPage = 14;
        })
        .AppendCss("table-hover")
    )
</div>
