@model IEnumerable<MvcFactbook.ViewModels.Models.Main.PoliticalEntitySucceedingView>

<div>
    @(Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add().Encoded(false).RenderedAs(model => $"<img src='/images/icons/succeedingclass-light.png' style='height: 20px; width: 20px' />");
            columns.Add(model => model.PrecedingEntity.Name).Titled("Preceding");
            columns.Add(model => model.SucceedingEntity.Name).Titled("Succeeding");
            columns.Add().Encoded(false).RenderedAs(model =>
                "<div class='btn-group float-left'>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntitySucceeding/Edit/{model.Id}'>Edit</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntitySucceeding/Details/{model.Id}'>Details</a>" +
                    $"<a class='btn btn-outline-secondary btn-sm' href='/PoliticalEntitySucceeding/Delete/{model.Id}'>Delete</a>" +
                "</div>"
            );
        })
        .Empty("No data found")
        .Filterable()
        .Sortable()
        .Pageable(pager =>
        {
            pager.RowsPerPage = 14;
        })
        .AppendCss("table-hover")
    )
</div>
