<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <img src="~/images/icons/ship-light.png" style="height:24px;width:24px" />
    <a class="navbar-brand pl-4 pr-4" href="#">Ship</a>
    <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" asp-action="Index">Index</a></li>
        <li class="nav-item"><a class="nav-link" asp-action="Edit" asp-route-id="@Model.Id">Edit</a></li>
        <li class="nav-item"><a class="nav-link" asp-action="Delete" asp-route-id="@Model.Id">Delete</a></li>
        <li class="nav-item"><a class="nav-link" asp-action="Details" asp-route-id="@Model.Id">Details</a></li>
        <!-- Dropdown -->
        <li class="nav-item dropdown">
            <a class="nav-link" href="#" id="navbardrop" data-toggle="dropdown">Lists</a>
            <div class="dropdown-menu">
                <a class="dropdown-item" asp-action="DetailsShipServices" asp-route-id="@Model.Id">Services</a>
            </div>
        </li>
        <li class="nav-item dropdown">
            <a class="nav-link" href="#" id="navbardrop" data-toggle="dropdown">Items</a>
            <div class="dropdown-menu">
                @if (Model.Builder != null)
                {
                    <a class="dropdown-item" asp-controller="Builder" asp-action="Details" asp-route-id="@Model.BuilderId">@Model.Builder.Name</a>
                }
                @foreach (var item in Model.ShipSubTypes)
                {
                    <a class="dropdown-item" asp-controller="ShipSubType" asp-action="Details" asp-route-id="@item.Id">@item.Type</a>
                }
                @foreach (var item in Model.ShipClasses)
                {
                    <a class="dropdown-item" asp-controller="ShipClass" asp-action="Details" asp-route-id="@item.Id">@item.Name</a>
                }
                @foreach (var item in Model.Branches)
                {
                    <a class="dropdown-item" asp-controller="Branch" asp-action="Details" asp-route-id="@item.Id">@item.Name</a>
                }
            </div>
        </li>
    </ul>
</nav>
