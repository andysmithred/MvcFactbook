@model MvcFactbook.ViewModels.Models.Main.ShipTypeView

<partial name="FleetItemDetails" model='new { Label = "TOTAL FLEET", FleetItem = @Model.Fleet.TotalFleet.FleetServicesTotal }' />

<hr class="bg-light" />

<partial name="FleetItemDetails" model='new { Label = "ACTIVE FLEET", FleetItem = @Model.Fleet.ActiveFleet.FleetServicesTotal }' />

<hr class="bg-light" />

<partial name="FleetItemDetails" model='new { Label = "INACTIVE FLEET", FleetItem = @Model.Fleet.InactiveFleet.FleetServicesTotal }' />

<hr class="bg-light" />
